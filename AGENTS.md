# Project Agent Instructions

- Use Python 3.10+.
- Keep code under `src/` and tests under `tests/`.
- Prefer type hints and dataclasses for core models.
- Provide Chinese documentation in `README.md` for user-facing features.
